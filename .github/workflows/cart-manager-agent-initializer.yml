name: Run Cart Manager Agent Initializer

on:
  push:
    branches:
      - main
    paths:
      - src/app/agents/cartManagerAgent_initializer.py
      - src/prompts/CartManagerPrompt.txt
  workflow_dispatch:

jobs:
  run-initializer:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create .env file from secret
        run: 'echo "$ENV" > src/.env'
        shell: bash
        env:
          ENV: ${{ secrets.ENV }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r src/requirements.txt

      - name: Validate azure.ai.projects import
        run: python -c "from azure.ai.projects import AIProjectClient"

      - name: Run cart manager agent initializer
        run: |
          cd src/
          python app/agents/cartManagerAgent_initializer.py